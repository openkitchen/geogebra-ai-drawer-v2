"""LLM utilities for v2 backend.

This package centralizes:
- model config loading from env
- role selection (capability-based)
- common invocation helpers (structured output + fallback)
- lightweight intent classification using a cheap model
"""


